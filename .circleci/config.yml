version: 2.1
jobs:
    build:
        docker:
            - image: circleci/node:v16.13.2
        steps:
            - checkout
            - run:
                name: Install dependencies
                command: npm install
            - run:
                name: Run linter
                command: npm run lint
            - run:
                name: Run test
                command: npm run test
